name: Build ollama-qwen05 Docker image

on:
  push:
    branches: [main]
    paths:
      - "ollama-qwen05/**"
      - ".github/workflows/build-ollama-qwen05.yml"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc \
            /opt/hostedtoolcache /usr/local/share/boost /usr/share/swift \
            /usr/local/graalvm /usr/local/share/chromium /usr/local/share/powershell \
            /usr/local/.ghcup /opt/az /usr/share/miniconda /usr/local/go \
            /usr/local/julia* /usr/local/sqlpackage /opt/microsoft /usr/local/aws-cli
          sudo apt-get purge -y -qq firefox google-chrome-stable \
            microsoft-edge-stable dotnet* aspnetcore* 2>/dev/null || true
          sudo apt-get autoremove -y -qq && sudo apt-get clean
          docker system prune -af
          echo "Free space:" && df -h /

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push (qwen2.5:0.5b pre-baked)
        uses: docker/build-push-action@v6
        with:
          context: ollama-qwen05
          file: ollama-qwen05/Dockerfile
          push: true
          tags: |
            marcosremar/ollama-qwen05:latest
            marcosremar/ollama-qwen05:${{ github.sha }}
            ghcr.io/marcosremar/ollama-qwen05:latest
            ghcr.io/marcosremar/ollama-qwen05:${{ github.sha }}
          cache-from: type=gha,scope=ollama-qwen05
          cache-to: type=gha,mode=max,scope=ollama-qwen05
